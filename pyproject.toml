[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "storyboard-asset-hub"
version = "0.1.0"
description = "分镜生成资产中枢：血缘、幂等任务、核验入库与崩溃恢复"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
