[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "storyboard-asset-hub"
version = "0.1.0"
description = "分镜生成任务与资产声明契约"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
